# desk-games

桌游合集：打开是一个游戏列表首页，点卡片进入具体游戏，可返回大厅。首发**掼蛋**（4 人 2v2，一期单人对 3 个 AI、规则零错），**中国象棋**作为内置联机模块接入（大厅内 mount，离开零残留）。纯 TypeScript 函数式规则引擎 + vitest 穷举/模糊单测保证"无 bug"，Vite 构建为可双击运行的单 HTML 文件，卡牌用 DOM+CSS。规则细节见 `SPEC.md`，技术约定见 `CLAUDE.md`。
