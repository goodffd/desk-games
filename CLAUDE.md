# desk-games：Claude 轨已停用

本项目已由 Codex 单轨全面接管，当前维护规则与项目技术约定统一见 `AGENTS.md`。

- 不使用 Claude/Anthropic API、CLI 或协作 worker。
- 不在本项目创建 `.claude/` 或 `.claude-collab/` 任务。
- 如果从 Claude Code 打开本仓库，请停止修改并转交 Codex。

保留本文件仅用于兼容旧入口和明确停用状态；它不再承载项目事实。
